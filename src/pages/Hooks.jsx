import React from 'react'
// import UseEffectExample from '../hooks/UseEffectExample';
import UseReducerExample from '../hooks/UseReducerExample';

const Hooks = () => {
  return (
    /** React Hooks are functions that let you "HOOK" into React Features
     * useState: useState is used to add State to react components
     * useState could be used for buttons 
     */


    <div>
        <UseReducerExample />
    </div>
  )
}

export default Hooks
